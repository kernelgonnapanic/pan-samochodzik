{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "./target/debug/pan_samochodzik",
            "cwd": "${workspaceRoot}"
        }
    ]
}